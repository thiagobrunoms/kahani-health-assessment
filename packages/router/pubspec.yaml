name: router
description: Router package for wellness app navigation
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ^3.9.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.6.2
  injectable: ^2.4.0
  authentication:
    path: ../authentication
  post:
    path: ../post
  common_dependencies:
    path: ../common_dependencies

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.9
  injectable_generator: ^2.6.0

flutter:
  uses-material-design: true

